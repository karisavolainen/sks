# Readme for a SKS website

Instructions.

* Clone this repo
* Make your changes
* Commit changes and push to github
```
$ git commit -a -m "message"
$ git push
```

* Push to production:
```
$ git push production master
```
